# Archivo: admin/config.yml (VERSIÓN FINAL)

backend:
  name: git-gateway 
  repo: cazaestilo/CazaEstilo  
  branch: main 

# ESTO ES CRUCIAL PARA GITHUB PAGES:
# Usamos el nombre del repositorio como ruta base
base_url: /CazaEstilo 
site_url: https://cazaestilo.github.io/CazaEstilo
display_url: https://cazaestilo.github.io/CazaEstilo

# Configuraciones de Medios
media_folder: "assets/img/products" 
# Usamos la ruta completa para la carpeta pública
public_folder: "/CazaEstilo/assets/img/products" 

collections:
  - name: "productos" 
    label: "Productos Destacados" 
    folder: "_productos" 
    create: true 
    slug: "{{slug}}"
    format: "json" 
    fields: 
      - {label: "Nombre del Producto", name: "title", widget: "string"}
      - {label: "Descripción Detallada", name: "body", widget: "markdown", required: true}
      - {label: "Precio (COP)", name: "price", widget: "number", value_type: "int", min: 0, step: 100}
      - {label: "Stock Disponible (0 = Agotado)", name: "stock", widget: "number", value_type: "int", min: 0, default: 1}
      - {label: "Calidad (%)", name: "quality", widget: "number", value_type: "int", min: 0, max: 100, default: 100}
      
      # BLOQUE DE IMÁGENES CORREGIDO: Usando el formato de indentación estándar
      - label: "Imágenes del Carrusel (Max 3)"
        name: "images"
        widget: "list"
        max: 3
        min: 1
        fields:
          # Las propiedades están correctamente separadas por comas
          - {label: "Imagen", name: "image", widget: "image", choose_url: false, allow_multiple: false}